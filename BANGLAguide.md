
# 🤖 ROBIN-AI-NEW (সম্পূর্ণ বাংলা সংস্করণ)
## এআই-চালিত ডার্ক ওয়েব OSINT টুল
---
### 🎬 ভিডিও ডেমো (CLICK IMAGE) ⤵️⤵️⤵️

[![ROBIN AI ভিডিও ডেমো দেখুন (ছবিতে ক্লিক করুন)](assets/thumnail.png)](https://t.me/robinainew/2)  
ROBIN-AI এর সম্পূর্ণ OSINT ক্ষমতার ২০ মিনিটের ভিডিও ডেমো দেখুন।

## 💻 Kali Linux (WSL) ইনস্টলেশন – উইন্ডোজে
উইন্ডোজ সাবসিস্টেম ফর লিনাক্স (WSL) দিয়ে কালি লিনাক্স সেটআপ করার ধাপসমূহ:

1. **ভার্চুয়াল মেশিন প্ল্যাটফর্ম চালু করুন**  
   ```powershell
   dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
   ```

2. **উইন্ডোজ সাবসিস্টেম ফর লিনাক্স (WSL) চালু করুন**  
   ```powershell
   dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
   ```

3. **মাইক্রোসফট স্টোর থেকে Kali Linux ইনস্টল করুন**  
   [Microsoft Store লিংক](https://apps.microsoft.com/detail/9pkr34tncv07?launch=true&mode=full&hl=en-us&gl=bd&ocid=bingwebsearch)

4. **WSL দিয়ে Kali Linux ইনস্টল করুন**  
   ```powershell
   wsl --install -d kali-linux
   ```

5. **Kali Linux চালু করুন**  
   ```powershell
   wsl -d kali-linux
   ```

---
## 🐳 Docker ইনস্টলেশন
ROBIN AI চালাতে Docker আবশ্যক।

1. **উইন্ডোজের জন্য Docker Desktop ডাউনলোড ও ইনস্টল করুন**  
   [Docker Desktop ডাউনলোড](https://docs.docker.com/desktop/setup/install/windows-install/)

2. **Kali-WSL এর সাথে Docker ইন্টিগ্রেশন সঠিকভাবে সেট করুন**  

   - **ডকার ডেস্কটপ অ্যাপ্লিকেশন চালু করুন:** উইন্ডোজে Docker Desktop অ্যাপটি ওপেন করুন।  
   - **সেটিংসে যান:** গিয়ার আইকন ⚙️ ক্লিক করে **Settings** খুলুন।  
   - **Resources সেকশনে যান:** বাম পাশের মেনু থেকে **Resources** সিলেক্ট করুন।  
   - **WSL Integration খুঁজুন:** Resources এর ভিতরে **WSL Integration** ট্যাবে ক্লিক করুন।  
   - **Kali-Linux এর ইন্টিগ্রেশন চালু করুন:** **kali-linux** এর পাশের টগল সুইচটি **ON** করে দিন।  
   - **সেটিংস সেভ করুন:** **Apply & Restart** বাটনে ক্লিক করুন (যদি দেখায়), তাহলে Docker রিস্টার্ট হয়ে সেটিংস সেভ হবে।
   
> 💡 প্রো টিপ: সবচেয়ে ভালো পারফরম্যান্সের জন্য সব WSL ডিস্ট্রিবিউশন অন করে রাখুন।

---
## ⚙️ ROBIN AI সেটআপ (ধাপে ধাপে)

1. **রেফারেন্স (ঐচ্ছিক)**  
   [NetworkChuck Dark Web Scraping Guide](https://github.com/theNetworkChuck/dark-web-scraping-guide)

2. **রিপোজিটরি ক্লোন করুন**  
   ```bash
   git clone https://github.com/apurvsinghgautam/robin.git
   ```

3. **ফোল্ডারে প্রবেশ করুন**  
   ```bash
   cd robin
   ```

4. **স্ক্রিপ্টগুলোকে এক্সিকিউটেবল করুন**  
   ```bash
   sudo chmod +x *
   ```

5. **প্যাকেজ লিস্ট আপডেট করুন**  
   ```bash
   sudo apt update
   ```

6. **Tor ইনস্টল করুন**  
   ```bash
   sudo apt install tor -y
   ```

7. **.env ফাইল তৈরি করুন**  
   ```bash
   cp .env.example .env
   ```

8. **.env ফাইল এডিট করুন**  
   ```bash
   nano .env
   ```

9. **OpenRouter Base URL** (এডিট করার সময় এটা রাখুন)  
   ```
   https://openrouter.ai/api/v1
   ```

10. **Docker ইমেজ বিল্ড করুন**  
    ```bash
    docker build -t robin .
    ```

11. **Gemini API কী নিন**  
    [Google AI Studio](https://aistudio.google.com/api-keys)

12. **OpenRouter প্ল্যাটফর্ম**  
    [OpenRouter ওয়েবসাইট](https://openrouter.ai/)

13. **মূল গিটহাব রিপোজিটরি**  
    [https://github.com/apurvsinghgautam/robin](https://github.com/apurvsinghgautam/robin)

---
## 🚀 ROBIN AI চালানো (স্ট্যান্ডার্ড ডকার)

15. ডিটাচড মোডে চালাতে চাইলে:

```bash
sudo docker run -d \
  --name robin \
  -p 8501:8501 \
  -v $(pwd):/app \
  --env-file .env \
  robin
```

16. RUN AI

```bash
sudo docker run --rm \
   -v "$(pwd)/.env:/app/.env" \
   --add-host=host.docker.internal:host-gateway \
   -p 8501:8501 \
   apurvsg/robin:latest ui --ui-port 8501 --ui-host 0.0.0.0
```

লেটেস্ট ইমেজ পুল করুন:

```bash
docker pull apurvsg/robin:latest
```

---
## 🔄 ROBIN AI ফুল আপডেট

### Keep your Robin installation current with these commands.

1.  **Update, Upgrade, and Navigate:**
    ```bash
    sudo apt update -y && sudo apt upgrade -y && cd robin
    ```
2.  **Pull Latest Changes from GitHub:**
    ```bash
    git pull origin main
    ```
3.  **Rebuild the Docker Image:**
    ```bash
    docker build -t robin .
    ```
4.  **Pull the Latest Pre-built Image:**
    ```bash
    docker pull apurvsg/robin:latest

```

```
## ▶️ প্রতিদিনের কাজের ফ্লো (Daily Workflow)

| ধাপ | কাজ | কমান্ড / লিংক |
|-----|-----|---------------|
| ১ | Kali Linux WSL চালু করুন | `wsl -d kali-linux` |
| ২ | Robin আপডেট করুন | উপরের **Robin Full Update** এর সব কমান্ড চালান |
| ৩ | Docker Desktop চালু করুন | Docker Desktop অ্যাপ ওপেন করুন |
| ৪ | Robin AI চালান | **কমান্ড নম্বর ১৬** (উপরের সবচেয়ে বড় কমান্ড) চালান |
| ৫ | ওয়েব ইন্টারফেসে প্রবেশ করুন | ব্রাউজারে যান: [http://localhost:8501](http://localhost:8501) |
| ৬ | Robin বন্ধ করুন | টার্মিনালে `Ctrl + C` চাপুন |
| ৭ | Kali WSL বন্ধ করুন | `wsl --terminate kali-linux` |
| ৮ | Docker বন্ধ করুন | `wsl --terminate docker-desktop` |

---

**সম্পূর্ণ প্রস্তুত! এখন আপনি ডার্ক ওয়েব OSINT এর জন্য সবচেয়ে শক্তিশালী টুল ROBIN-AI ব্যবহার করতে পারবেন।**


উপভোগ করুন! 🔥  
তৈরি করেছেন: Apurv Singh Gautam  
```
